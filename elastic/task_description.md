## 📄 Методическое Руководство: Расчет Тензора Упругости Твердого Тела в LAMMPS

Расчет тензора упругости ($\mathbf{C}$) в LAMMPS,
использует метод **конечной деформации** (finite strain method).
Этот метод вычисляет производные компонент тензора напряжений ($\sigma$)
по малым компонентам деформации ($\epsilon$).

Тензор упругости в нотации Фойгта (Voigt notation) связывает изменение напряжений
с деформациями по закону Гука: $$\Delta\sigma_{i} = C_{ij}\epsilon_{j}$$
где $i, j \in \{1, 2, \dots, 6\}$. Компоненты тензора $C_{ij}$ вычисляются как:
$$C_{ij} = \left(\frac{\partial\sigma_i}{\partial\epsilon_{j}}\right)$$
На практике $C_{ij}$ определяется путем применения малой положительной ($\epsilon$)
и отрицательной ($-\epsilon$) деформации в направлении $j$ и измерения соответствующего
изменения напряжений $\Delta\sigma_i$.

***

## 🛠️ Общий Процесс Расчета

Процесс расчета, состоит из четырех основных этапов:

### 1. Подготовка Исходных Данных

* **Определение Единиц и Конверсионного Фактора:** Выбирается система единиц LAMMPS (`units`).
  Для конвертации из единиц давления LAMMPS в выходные единицы упругих констант (например, **ГПа**)
  устанавливается коэффициент **cfac**.
    * *Пример:* Использование `units metal` и `cfac equal 1.0e-4` для получения результатов в **ГПа**.
* **Определение Исходной Структуры:** Задаются параметры кристаллической решетки (например, `lattice diamond`),
  размеры ячейки (`region`), и создаются атомы.
* **Параметры Деформации и Минимизации:**
    * **up**: Величина конечной деформации (strain units).
      Рекомендуется проверить, что результаты **не зависят** от этого значения.
    * **etol**, **ftol**, **maxiter**, **maxeval**: Критерии сходимости для минимизации энергии.
      При отсутствии необходимости в минимизации, можно установить `maxiter = 0.0`.

***

### 2. Определение Потенциала

* **Pair Style:** Выбор потенциала (например, `pair_style sw` для потенциала **Stillinger-Weber**).
* **Pair Coeff:** Установка параметров потенциала для конкретного материала.

***

### 3. Эквилибрация и Расчет Исходного Состояния

Система приводится к равновесному состоянию при нулевом давлении:

1.  **Релаксация Ячейки и Атомов:**
   Выполняется релаксация ячейки с помощью команды `fix 3 all box/relax aniso 0.0`.
3.  **Минимизация Энергии:**
   Выполняется минимизация энергии для достижения равновесного положения атомов (`minimize`).
5.  **Сохранение Исходных Параметров:**
   Сохраняются компоненты тензора напряжений ($\mathbf{P}_0$: `pxx0`, `pyy0`, ...)
   и размеры ячейки ($\mathbf{L}_0$: `lx0`, `ly0`, ...) в равновесном состоянии.
7.  **Создание Restart-Файла:** Создается файл `restart.equil`
   для последующего многократного использования.

***

## 4. Применение Деформаций и Измерение Напряжений

Этот этап является ключевым для расчета тензора упругости методом конечной деформации.
Он циклически повторяется **6 раз** — по одному разу для каждой независимой компоненты
деформации $\epsilon_{j}$ в нотации Фойгта, где $j \in \{1, \dots, 6\}$ соответствует направлениям $xx, yy, zz, yz, xz, xy$.

Для каждого направления $j$ (задается переменной $\text{dir}$),
скрипт выполняет два цикла: для отрицательной ($-\epsilon$) и положительной ($+\epsilon$) деформации,
измеряя все 6 компонент тензора напряжений $\sigma_{i}$ для каждого случая.

### Шаг 4.1: Сброс и Загрузка Равновесного Состояния

В начале каждого цикла деформации скрипт гарантирует, что система находится в исходном неискаженном равновесном состоянии:

1.  **Очистка и Загрузка:** Команды `clear` и `read_restart restart.equil` загружают геометрию
   и параметры ячейки из файла, сохраненного на Шаге 3, который соответствует **нулевой деформации ($\epsilon=0$)**.
3.  **Длина Ячейки:** Определяется равновесная длина ячейки ${len0}$ (например, `lx0`, `ly0`, `lz0`),
   соответствующая текущей компоненте $\text{dir}$.
5.  **Величина Деформации:** Рассчитывается абсолютное изменение размера ячейки/параметра наклона $\text{delta} = \pm {up} \cdot {len0}$.

***

### Шаг 4.2: Изменение Ячейки (Команда `change_box`)

Команда `change_box` используется для применения малой, **фиксированной деформации** к границам расчетной ячейки.

1.  **Применение Отрицательной Деформации ($-\epsilon$):**
    * Устанавливаются отрицательные значения $\text{delta}$ и соответствующих параметров сдвига (`deltaxy`, `deltaxz`, `deltayz`).
    * Выполняется команда `change_box all <параметры> remap units box`. Параметр `remap units box` критически важен: он гарантирует, что атомы внутри ячейки **перемещаются пропорционально деформации ячейки**, сохраняя свои относительные координаты.
    * **Примеры команд** `change_box` для разных $\text{dir}$:
        * **$\text{dir}=1$ ($\epsilon_{xx}$):** `change_box all x delta 0 ${delta} xy delta ${deltaxy} xz delta ${deltaxz} remap units box`
        * **$\text{dir}=4$ ($\epsilon_{yz}$):** `change_box all yz delta ${delta} remap units box`

2.  **Релаксация Положений Атомов:**
    * Поскольку атомы переместились вместе с ячейкой, они теперь находятся не в равновесных положениях относительно новой геометрии.
    * Выполняется **минимизация энергии** (`minimize ${etol} ${ftol} ${maxiter} ${maxeval}`). **Размеры и форма ячейки при этом остаются фиксированными**. Релаксируют только **координаты атомов** до достижения нового механического равновесия при заданной деформации.

***

### Шаг 4.3: Измерение Напряжений $\mathbf{P}$

После минимизации система находится в равновесии при деформации $\epsilon_{j}$. Измеряются все 6 компонент тензора напряжений (в LAMMPS это переменные `pxx`, `pyy`, `pzz`, `pxy`, `pxz`, `pyz`):

| Компонента напряжений $i$ | Переменная LAMMPS | Сохраняемое значение |
| :---: | :--- | :--- |
| $\sigma_{xx}$ | `pxx` | `pxx1` |
| $\sigma_{yy}$ | `pyy` | `pyy1` |
| $\sigma_{zz}$ | `pzz` | `pzz1` |
| $\sigma_{xy}$ | `pxy` | `pxy1` |
| $\sigma_{xz}$ | `pxz` | `pxz1` |
| $\sigma_{yz}$ | `pyz` | `pyz1` |

Эти значения $\sigma_{i}^{(-)}$ (например, `pxx1`) используются для расчета первой оценки $j$-го столбца тензора упругости $C_{i, j}^{\text{neg}}$ по формуле:
$$C_{i, j}^{\text{neg}} = \frac{\sigma_{i}^{(-)} - \sigma_{i}^{(0)}}{-\epsilon_{j}} \cdot c_{\text{fac}}$$
Где $\sigma_{i}^{(0)}$ — напряжения в исходной равновесной ячейке.

***

### Шаг 4.4: Повторение с Положительной Деформацией

Шаги 4.1-4.3 повторяются с **положительной деформацией** ($+\epsilon$):
* Ячейка снова сбрасывается к `restart.equil`.
* Применяется `change_box` с $\text{delta} = +up \cdot len0$.
* Выполняется минимизация.
* Измеряются напряжения $\mathbf{P}^{(+)}$ (например, `pxx2`).
* Вычисляется вторая оценка $C_{i, j}^{\text{pos}}$.

***

### Шаг 4.5: Окончательное Вычисление Столбца $C_{i, j}$

Для каждой компоненты $i$ в столбце $j$ усредняются две оценки, полученные из положительной и отрицательной деформаций (метод центральной разности), что повышает точность:

$$C_{i, j} = 0.5 \cdot (C_{i, j}^{\text{neg}} + C_{i, j}^{\text{pos}})$$

После прохождения цикла для всех $\text{dir}=1\dots6$, вы получаете все 36 компонент тензора $\mathbf{C}$.

***

## 📈 Анализ Результатов

После вычисления всех $C_{i, \text{dir}}$, выполняется постобработка для получения финальных значений и макроскопических модулей.

### 1. Финальные Компоненты

* **Усреднение по симметрии:** Недиагональные элементы усредняются для обеспечения симметрии тензора $C_{ij} = C_{ji}$.
    * $$C_{12\text{all}} = 0.5(C_{12} + C_{21})$$
    * $$C_{45\text{all}} = 0.5(C_{45} + C_{54})$$

### 2. Средние Модули для Кубического Кристалла

Для кубической симметрии, как в примере с Stillinger-Weber кремнием, рассчитываются усредненные значения трех независимых констант:

* $$C_{11}^{\text{cubic}} = \frac{C_{11\text{all}} + C_{22\text{all}} + C_{33\text{all}}}{3.0}$$
* $$C_{12}^{\text{cubic}} = \frac{C_{12\text{all}} + C_{13\text{all}} + C_{23\text{all}}}{3.0}$$
* $$C_{44}^{\text{cubic}} = \frac{C_{44\text{all}} + C_{55\text{all}} + C_{66\text{all}}}{3.0}$$

### 3. Производные Макроскопические Модули

На основе усредненных констант вычисляются ключевые механические свойства:

| Модуль | Формула |
| :--- | :--- |
| **Модуль всестороннего сжатия** (Bulk Modulus, $K$) | $$K = \frac{C_{11}^{\text{cubic}} + 2C_{12}^{\text{cubic}}}{3.0}$$ |
| **Модуль сдвига 1** (Shear Modulus 1, $G_1$) | $$G_1 = C_{44}^{\text{cubic}}$$ |
| **Модуль сдвига 2** (Shear Modulus 2, $G_2$) | $$G_2 = \frac{C_{11}^{\text{cubic}} - C_{12}^{\text{cubic}}}{2.0}$$ |
| **Коэффициент Пуассона** ($\nu$) | $$\nu = \frac{1.0}{1.0 + C_{11}^{\text{cubic}}/C_{12}^{\text{cubic}}}$$ |

***

## ⚠️ Рекомендации по Сходимости

* **Конвергенция Минимизации:** Крайне важно убедиться, что минимизация энергии в соотвествии с положениями атомов в деформированной ячейке полностью сошлась.
* **Выбор $\text{up}$:** Проверьте, что результаты расчета упругих констант **нечувствительны** к выбору значения переменной `up`.
* **Проверка Сил:** Одно из указаний на сходимость — это проверка финальной максимальной и двунормной силы (`max` and `two-norm forces`) в log-файле после минимизации.

## 🐍 Подробности Вызова LAMMPS с Помощью Python

Основное преимущество использования Python-обёртки LAMMPS заключается в возможности **динамического управления симуляцией** и **прямого доступа к внутренним данным** (таким как термодинамические переменные, массивы атомов, размеры ячейки) без записи промежуточных файлов.

### 1. Инициализация и Управление Симулятором

Код `elastic.py` использует библиотеку `lammps` следующим образом:

* **Инициализация (строка 76):**
    ```python
    lmp = lammps()
    ```
    Создание экземпляра класса `lammps` (**`lmp`**) запускает симулятор LAMMPS в памяти как отдельный процесс или объект. Переменная `lmp` становится **единственной точкой доступа** ко всем командам и данным LAMMPS.

* **Передача Команд (строки 80-84, 95-97):**
    ```python
    lmp.commands_string(f"""
        units        metal
        # ...
        create_atoms 1 box
    """)
    # ...
    lmp.command("write_restart restart.equil")
    ```
    Методы `lmp.commands_string()` и `lmp.command()` служат для отправки **стандартных команд LAMMPS** в запущенный симулятор, точно так же, как если бы они были записаны в традиционном входном файле. F-строки Python позволяют легко **встраивать переменные** (например, `params["a"]`) из Python-кода непосредственно в команды LAMMPS.

* **Закрытие Симулятора (строка 149):**
    ```python
    lmp.close()
    ```
    Метод `lmp.close()` корректно завершает работу экземпляра LAMMPS и освобождает связанные ресурсы.

---

### 2. Извлечение Данных LAMMPS в Python

Ключевой элемент, обеспечивающий **итеративный расчет** тензора $\mathbf{C}$, — это способность **извлекать результаты** симуляции после каждого шага деформации и минимизации.

* **Извлечение Термодинамических Переменных (строки 12, 114):**
    ```python
    def extract_thermo(lmp, keys):
        return {key: lmp.get_thermo(key) for key in keys}
    # ...
    pressure = extract_thermo(lmp, PRESSURE_COMPONENTS)
    ```
    * Метод **`lmp.get_thermo(key)`** — это API-функция для извлечения *последнего* значения указанной термодинамической переменной, вычисленной LAMMPS.
    * В данном скрипте он используется для получения **компонент давления/напряжения** (`pxx`, `pyy`, ...) и **размеров ячейки** (`lx`, `ly`, `lz`) после минимизации, которые являются критически важными данными для расчета $C_{ij}$.

* **Извлечение Параметров Ячейки (строка 33):**
    ```python
    (xy, xz, yz) = lmp.extract_box()[2:5]
    ```
    Метод **`lmp.extract_box()`** возвращает массив данных о текущем состоянии расчетной ячейки, включая **параметры наклона** (tilt parameters) $xy$, $xz$, и $yz$. Эти параметры необходимы для расчета соответствующих **сдвиговых деформаций** (`d_xy`, `d_xz`, `d_yz`) в функции `displace`.

---

### 3. Циклическое Управление Деформацией (Функция `displace`)

Функция `displace` инкапсулирует Шаги 4.1–4.4 Методического Руководства и является сердцем **метода конечной деформации**.

* **Сброс Ячейки (строки 35–38):**
    ```python
    # ...
    lmp.commands_string("""
        clear
        box tilt large
        read_restart restart.equil
    """)
    ```
    Перед каждой новой деформацией система сбрасывается к **исходному равновесному состоянию** ($\epsilon=0$), загруженному из файла `restart.equil`. Это гарантирует, что каждая деформация начинается с неискаженной ячейки, делая расчет независимым для каждого $C_{ij}$ столбца.

* **Применение Деформации (строки 48-61):**
    ```python
    if dir_val == 1:
        lmp.command(
            f"change_box all x delta 0 {d} xy delta {d_xy} xz delta {d_xz} remap units box"
        )
    # ...
    ```
    Python-переменные (`d`, `d_xy`, `d_xz`, `d_yz`) сначала рассчитываются в Python (строки 44-46), а затем используются для **динамического формирования** и отправки команды `change_box` в LAMMPS. Таким образом, Python контролирует **величину и тип** приложенной деформации.

* **Минимизация (строка 62):**
    ```python
    lmp.command(
        f"minimize {params['etol']} {params['ftol']} {params['maxiter']} {params['maxeval']}"
    )
    ```
    После деформации запускается команда `minimize`. Важно, что **ячейка остается зафиксированной** (потому что `change_box` не использовал опцию `box/relax`), и релаксируют только **координаты атомов** внутри деформированной ячейки.

* **Расчет Столбца $C_{i, j}$ (строки 66–70):**
    ```python
    pressure = extract_thermo(lmp, PRESSURE_COMPONENTS)
    strain = d / len0
    return [
        -(pressure[key] - initial_pressure[key]) / strain * params["cfac"]
        for key in PRESSURE_COMPONENTS
    ]
    ```
    Сразу после минимизации извлекаются напряжения. Python выполняет **финальный расчет** для $j$-го столбца тензора упругости:
    $$C_{i, j}^{\text{neg/pos}} = \frac{\sigma_{i}^{\text{def}} - \sigma_{i}^{(0)}}{\epsilon_{j}} \cdot c_{\text{fac}}$$
    Где $\epsilon_{j}$ — это переменная `strain`, $\sigma_{i}^{\text{def}}$ — `pressure[key]`, а $\sigma_{i}^{(0)}$ — `initial_pressure[key]`.

---

### 4. Постобработка (Функция `calculate_elastic_constants`)

Python используется для **агрегации** результатов и выполнения **постобработки**:

* **Сбор Данных (строки 118–127):**
    ```python
    C_neg = np.array(
        [displace(lmp, params, box_dims, pressure_initial, dir_val, -1.0) for dir_val in range(6)]
    )
    # ...
    C_matrix = 0.5 * (C_neg + C_pos).T
    ```
    Функция `displace` вызывается циклически **12 раз** (6 направлений $\times$ 2 знака деформации). Результаты собираются в **`numpy` массивы** (`C_neg`, `C_pos`), что позволяет легко выполнять **матричные операции**, такие как **усреднение** и **транспонирование**, для получения окончательной матрицы $\mathbf{C}$.

* **Вычисление Макроскопических Модулей (строки 131–136):**
    ```python
    C11cubic = np.average(C_diag[0:3])
    # ...
    bulkmodulus = (C11cubic + 2 * C12cubic) / 3.0
    # ...
    ```
    Все формулы для усреднения констант кубического кристалла и расчета макроскопических модулей (**Модуль всестороннего сжатия**, **Модули сдвига**, **Коэффициент Пуассона**) **реализованы на чистом Python/NumPy**. Это позволяет выполнять сложный анализ результатов непосредственно в скрипте без необходимости использовать внешние инструменты.

Таким образом, Python-обертка LAMMPS позволяет выполнять расчет тензора упругости как **целостный, высокоавтоматизированный процесс**, где Python обеспечивает **управляющую логику** (циклы, условные операторы, расчет параметров деформации) и **анализ данных** (`numpy`), а LAMMPS — **физическое моделирование** (симуляция атомов, минимизация энергии, расчет напряжений).
